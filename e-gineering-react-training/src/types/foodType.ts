export type Food = {
  id: string;
  name: string;
  quantity: number;
  minQuantity: number;
  type: string;
};

export type NewFood = {
  name: string;
  quantity: number;
  minQuantity: number;
  type: string;
};
